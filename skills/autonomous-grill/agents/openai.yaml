interface:
  display_name: "Autonomous Grill"
  short_description: "Cross-model grilling with human verdicts"
  default_prompt: "Use $autonomous-grill to stress-test this plan across model families, then walk me through the resulting decisions."
policy:
  allow_implicit_invocation: false
